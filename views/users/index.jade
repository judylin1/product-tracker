extends ../layout

block content
  include ../partials/header
  main
    h4 Purchased Products
    for product in products
      p
        a(href='/users/'+cookieId+'/products/'+ product._id) !{product.name}
    a.button(href='/users/'+ cookieId + '/products/new') New
